[advisories]
# Empty on purpose: every advisory this crate pulls in is fixed by a version bump or by
# dropping the feature that pulled the vulnerable crate in. An entry here is a last resort,
# and carries the reason and the condition for its removal.
ignore = []
